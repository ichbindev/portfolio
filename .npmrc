# Keep pnpm's strict, symlinked node_modules. Do NOT add shamefully-hoist:
# this repo previously depended on phantom (undeclared) packages via Yarn's
# flat hoisting, and strictness is what prevents that from recurring.
strict-peer-dependencies=false
